---
import { getCollection } from "astro:content";
import Base from "../../layouts/Base.astro";
import { buildGraph, nodesByKind } from "../../lib/graph";

const pages = await getCollection("pages");
const sources = await getCollection("sources");
const { nodes } = buildGraph(pages, sources);
const places = nodesByKind("place", nodes.values());
---
<Base
  title="Places — Catalog — Gourmet Gastronomer"
  description="Atlas places in Gourmet Gastronomer."
  permalink="/catalog/places/"
  crumbs={[
    { name: "Catalog", url: "/catalog/" },
    { name: "Places", url: "/catalog/places/" },
  ]}
  currentNav="catalog"
>
  <main class="home" id="main">
    <p class="eyebrow">Catalog</p>
    <h1>Places</h1>
    <ul class="catalog-list">
      {places.map((item) => (
        <li>
          <a href={item.url}>{item.data.title}</a>
          {" — "}
          {item.data.summary}
        </li>
      ))}
    </ul>
  </main>
</Base>
